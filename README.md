# Task Tracker CLI using node.js

# To run:

- npm install
- npm link

# Commands:

- Add a task: task add "task"
- Update a task: task update id "task"
- Update task status: task mark id "status"
- Delete a task: task delete id
- List all tasks: task list
- List tasks by status: task list "status"
